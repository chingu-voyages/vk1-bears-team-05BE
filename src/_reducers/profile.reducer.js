import { profileConstants } from "../_constants";

const initialState = {
  profile: null,
  filtered: null,
  current: null,
  loading: false,
  errors: {},
};

export const profile = (state = initialState, action) => {
  switch (action.type) {

  }
};
